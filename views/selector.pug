extends layout

block content
  h1= title
  - let image_names = ['valley1.jpg','valley2.jpg','valley3.jpg','valley4.jpg','valley5.jpg']
  - for(let i=0;i<3;i++){
      - let rno = Math.floor(Math.random()*5)
      - let iname = image_names[rno]

      img(src=`images/${iname}` alt='valley' style='width:300px; height:300px;')
      |    
  - }